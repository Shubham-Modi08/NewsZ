<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewsZ" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorPrimaryVariant">@color/my_light_primary</item> <!-- Variant of the primary color -->
        <item name="colorOnPrimary">@color/white</item> <!-- Text color on primary -->
        <item name="colorAccent">@color/my_light_accent</item> <!-- Accent color -->
        <item name="colorControlHighlight">@color/my_light_accent</item> <!-- Highlight color -->
        <item name="actionOverflowButtonStyle">@style/MyOverflowButtonStyle</item>
    </style>

    <style name="MyOverflowButtonStyle" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/white</item>
    </style>
    <style name="Theme.NewsZ" parent="Base.Theme.NewsZ" />
</resources>